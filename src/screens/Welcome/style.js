import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container:{
        borderStartColor: "#09579E",
        flex:1,
        alignItems:"center",
        justifyContent:"center"
    }
}) 